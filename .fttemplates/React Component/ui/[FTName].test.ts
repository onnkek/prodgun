import { render, screen } from '@testing-library/react';
import { [FTName] } from 'shared/ui/[FTName]/ui/[FTName]';

describe('[FTName]', () => {

  test('Test render', () => {
    //render();
    //expect(screen.getByText('')).toBeInTheDocument();
  });

});